schema:
  - ${EXPO_PUBLIC_APPSYNC_ENDPOINT}:
      headers:
        x-api-key: ${APPSYNC_API_KEY}

generates:
  src/generated/schema.ts:
    plugins:
      - typescript

  graphql.schema.json:
    plugins:
      - introspection
